.claude/*
!.claude/skills/

# The two rules above are anchored to the repo root. These are not - they keep local
# secrets and worktree checkouts ignored inside ANY .claude/ in the tree.
**/.claude/settings.local.json
**/.claude/worktrees/
.vercel

# Personal planning artifacts kept at repo root (not source)
GetInShape-Roadmap.pdf

# Android/TWA build (Bubblewrap) — never commit signing keys or build output
*.keystore
android.keystore
app-release-*.aab
app-release-*.apk
app-release-*.idsig
/build/

# Bubblewrap-generated Android project. Regenerated from twa-manifest.json by
# `bubblewrap update`, so the manifest is the source of truth, not this output.
# Paths are root-anchored on purpose: a bare `app/` would also swallow
# health-app/app/, which is the Next.js source.
/app/
/gradle/
/.gradle/
/gradlew
/gradlew.bat
/build.gradle
/settings.gradle
/gradle.properties
/manifest-checksum.txt
/store_icon.png
